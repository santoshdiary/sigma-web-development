react
1. react dom - for webapp
2. reac native- for mobileapp

JSX- JavaScript XML file

React project is created with the help of utility called npx (node package Execute) OLD WAY


 1...............NPM (Node Package Manager)................ {

NPM is the default package manager that comes with Node.js.
Think of it as an app store for JavaScript packages.

Using NPM, you can:

Install libraries (React, Express, Axios, etc.)
Update packages
Remove packages
Manage project dependencies
}

-------Creation of new React Project - 
npm create vite@latest (latest way using vite a utility)

by default module folder is missing, so we use - "npm install"
to install all dependencies that require 
to run project - npm run dev(script on package.json) 



------------------------HOW REACT WORKS?-------------

React creates a copy of real dom called virtual dom and compare it with real dom & 
eventually Update only that element or components instead updating whole dom.


********** Understand the struture of react project ********** 

Here, App.jsx file is  basically a js file that consists a function (javascript + html)
and it's exported then, imported by the main.jsx file which actually render the whole scripts
on index.html using target id "root" on index file. 


----------- DAY 3 LEARNINGS--------

1. while making components (jsx file), always capitalize name (function)
2. file extension .jsx, bc some libraries force us that .
3. while returning jsx we use empty <> called "Fragment" to bundle the more than one element,
 since can return only one element 
<>
  <h1> Hello world </h1>
   <p>this is a paragraph....</p>
</>

4. can create our own components like chai.jsx


----------PROP (PROPPERTIES) IN REACT -----
To pass data from a parent component down to a child component that make us to reuse a compontents with different values. 

(Props, which is short for properties, is the way for parent components to pass data down to the child component. Props can be of any type: strings, numbers, booleans, objects, or arrays. PROPS BASICALL AN "OBJECT" THAT PASSSES IN COMPONENET) 

useState hook's setter works in batch to reflect the output in screen that's why calling mutlitple time calling this function will not reflect output on screen different. 

Remember the counter interview problem in this series. 

